// Compile-time constants injected by Vite's `define` (see vite.config.ts).

// The app version, sourced from client/package.json at build time. Rendered in
// the top bar as "Athena v{__APP_VERSION__}".
declare const __APP_VERSION__: string
